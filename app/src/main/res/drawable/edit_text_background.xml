<!-- res/drawable/edit_text_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Цвет фона -->
    <solid android:color="@android:color/white" />

    <!-- Обводка -->
    <stroke
        android:width="1dp"
        android:color="@color/gray" /> <!-- Замените @color/gray на ваш цвет -->

    <!-- Закругленные углы -->
    <corners android:radius="8dp" /> <!-- Радиус закругления углов -->
</shape>